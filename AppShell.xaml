<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ecommerce_app.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ecommerce_app"
    Title="ecommerce_app">
    <TabBar>

        <!-- <ShellContent
            Title="Login Page"
            ContentTemplate="{DataTemplate local:Pages.Auth.LoginPage}"
            Route="Pages.Auth.LoginPage"
            Shell.NavBarIsVisible="False"/> -->
            
        <!-- <ShellContent
            Title="OTP Page"
            ContentTemplate="{DataTemplate local:Pages.Auth.OTPPage}"
            Route="Pages.Auth.OTPPage"
            Shell.NavBarIsVisible="False"/> -->

        <!-- <ShellContent
            Title="Products Page"
            ContentTemplate="{DataTemplate local:Pages.Ecommerce.ProductsPage}"
            Route="Pages.Ecommerce.ProductsPage"
            Shell.NavBarIsVisible="False"/> -->

            <ShellContent
            Title="Home"
            Icon="home.png"
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage"
            Shell.NavBarIsVisible="False"/>
                        
            <ShellContent
            Title="Settings"
            Icon="settings.png"
            ContentTemplate="{DataTemplate local:Pages.Auth.Settings}"
            Route="Settings"
            Shell.NavBarIsVisible="False"/>
       
    </TabBar>
</Shell>
